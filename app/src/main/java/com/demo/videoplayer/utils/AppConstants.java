package com.demo.videoplayer.utils;

import android.support.v4.media.session.PlaybackStateCompat;

public class AppConstants {

    public final static int CURRENT_STATE_NONE = PlaybackStateCompat.STATE_NONE;
    public final static int CURRENT_STATE_STOPPED = PlaybackStateCompat.STATE_STOPPED;
    public final static int CURRENT_STATE_PAUSED = PlaybackStateCompat.STATE_PAUSED;
    public final static int CURRENT_STATE_PLAYING = PlaybackStateCompat.STATE_PLAYING;

}
